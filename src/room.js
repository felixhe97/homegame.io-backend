// specific socketIO links/rooms/namespaces, one to one with autogenerated link
// when players join, can change their options here
// deals with chat

class Room {
    constructor(hostID, linkURL, roomPassword = 0, maxNum = 20) {
        this.hostID = hostID;
        this.linkURL = linkURL;
        this.dateCreated = new Date();
        if (!maxNum || maxNum < 2 || maxNum > 20) {
            this._maxOccupancy = 20;
        } else {
            this._maxOccupancy = maxNum;
        }
        if (!roomPassword) {
            this._password = undefined;
        } else {
            this._password = roomPassword;
        }
        this.occupants = new Map();
    }

    joinAndSpectate(player) {
        if (!this.occupants.get(player.name)) {
            this.occupants.set(player.name, player);
        }
    }

    spectate(playerID) {
        if (playerID) {
            // join chat
        }

    }

    sit() {

    }
}